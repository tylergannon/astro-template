decision: The user explicitly requested the experimental rsvelte toolchain; use its Vite compiler override plus native formatter, linter, and checker, and prove the generated shadcn-svelte corpus against them.
decision: Keep the official Svelte runtime and Astro integration, but redirect the integration's @sveltejs/vite-plugin-svelte dependency to @rsvelte/vite-plugin-svelte so Astro registers only one Svelte compiler plugin.
decision: Keep TypeScript 6 as Astro's supported JavaScript compiler peer and install the TypeScript native preview separately so rsvelte-check can use tsgo.
friction: `shadcn-svelte add --all` installed 55 Vega items and the styled Vega URL rejected `data-table`, but the generic registry index exposes `data-table` as a 56th `registry:ui` item -> install its direct registry URL and verify all 56 sources.
decision: Disable Vite+'s raw TypeScript diagnostic pass and make `rsvelte-check --tsgo` the type owner; raw ts-go sees only Astro's wildcard `.svelte` declaration and falsely rejects named module exports, while rsvelte-check supplies ts-go with generated TSX overlays.
friction: Vite+ stores Oxfmt options in vite.config.ts, but rsvelte-fmt invokes standalone Oxfmt and cannot read that block; mirror the singleQuote setting in .oxfmtrc.json so both required format gates converge.
friction: rsvelte-fmt's in-process JS/CSS formatting still diverges from Vite+ even with the same Oxfmt config -> use rsvelte's documented --no-native-js and --no-native-css escape hatches so rsvelte formats .svelte files and delegates other formats to the workspace Oxfmt binary.
friction: rsvelte-check did not discover the Astro tsconfig and generated an overlay without aliases or skipLibCheck, producing 1,239 false errors -> pass --tsconfig tsconfig.json explicitly; the same full corpus then reports zero diagnostics through ts-go.
decision: Keep rsvelte-lint's recommended correctness rules but disable style rules incompatible with source-installed shadcn/Tailwind or Astro routing; the unconfigured linter reported 610 warnings despite zero errors.
correction: Release proof must exercise the compiled preview server, a Svelte island authored in an Astro page, multiple generated shadcn-svelte components, and client-side hydration before merge and versioning.
decision: Keep the recommended rsvelte lint rules for first-party source and isolate shadcn-generated suppressions in rsvelte-lint.generated.json.
decision: rsvelte-check's displayed file count covers Svelte inputs, not total ts-go coverage; a deliberate TypeScript error in a temporary .ts file was caught by the existing rsvelte-check --tsgo gate.
correction: Keep first-party Svelte scripts typed and configure svelte/block-lang to require `ts`; removing `lang="ts"` can silently drop JavaScript blocks from ts-go checking.
decision: Exclude `ephemeral/**` proof, worklog, and review prose from Oxfmt so writing required evidence cannot make the code-format gate red.
correction: CI and agent instructions must invoke `vp run verify`; the old pre-Svelte commands omitted rsvelte formatting, linting, ts-go, and full registry compilation.
decision: Author verification programs as TypeScript included by rsvelte-check --tsgo, and keep preview assertions structural so ordinary template extension remains green.
correction: Disable Tailwind automatic root scanning and explicitly source `src/`; tracked proof and review prose must never generate production utilities or perturb asset hashes.
friction: rsvelte 0.5.2 miscompiles mode-watcher's multiline class-field ternary after a $state field during SSR -> patch that expression to one line, render Sonner in the real demo, and keep dependencies bundled in the registry SSR gate.
correction: Keep dependency regressions in the registry gate instead of permanently mounting Sonner in the public starter; document the same rsvelte 0.5.2 limitation for first-party rune modules and make its expected-failure probe signal when the workaround can be removed.
correction: A healthy upstream compiler fix must not red-line the release gate -> use working one-line and no-rune controls to discriminate the known bug, and make the expiry signal a non-blocking removal notice.
